export const AlbumsModal = ({album, isOpen, onClickCallback}) => {
    return album !== null && isOpen && <div className="fixed z-20 top-0 left-0 w-full h-full bg-black/50">
        <div className="text-white">MODAL WIP</div>
    </div>
}

